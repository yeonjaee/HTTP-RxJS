// RxJs map(), tap() 의 차이점

// map() 메서드는 새로운 객체를 반환한다.

const ex => ex + 1

// 10 이 전달되면 11 을 리턴하고, 11 을 전달하면 12 를 리턴한다.
// 그렇다면 map 은 왜 사용할까?
// 관찰되는 모든 옵저버블의 리턴 값(방출되는 값)에 어떠한 함수를 적용하고 새로운 값을 받을 수 있기 때문에.
/**
* 반면 tap() 메서드는 새로운 객체를 반환하지 않고, 원 데이터를 반환한다.
*/

// 그렇다면 tap 은 왜 사용할까?
// This operator is generally used for debugging observables for the correct values or performing other side effects.
